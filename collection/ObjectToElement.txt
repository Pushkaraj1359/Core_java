toArray()
Method 1
Object[] arr = family.toArray();

Converts ArrayList → Object Array.

Loop printing:

for(Object ob:arr)
    System.out.println(ob);

Output:

Navin
Sneha
Anup
Method 2
String names[] = new String[family.size()];
names = family.toArray(names);

Converts ArrayList → String Array.

Loop:

for(String name : names)
    System.out.println(name);

Output:

Navin
Sneha
Anup
1️⃣3️⃣ Iterator
Iterator<String> itr = family.iterator();

Iterator is used to traverse list elements one by one.

Loop:

while(itr.hasNext()){
    System.out.println(itr.next());
}

Explanation:

Method	Meaning
hasNext()	checks if next element exists
next()	returns next element

Output:

Navin
Sneha
Anup
